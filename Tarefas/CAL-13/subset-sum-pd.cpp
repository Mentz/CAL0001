#include <cstdio>
#include <cstdlib>
#include <vector>
#include <iostream>

vector<int> subset-sum(vector<int> set, int obj)
{
	vector<vector<bool> > pd(set.size(), vector<bool> (0)
}

int main(int argc, char ** argv)
{
	

	return 0;
}
