#ifndef __BIGINT_HPP__
#define __BIGINT_HPP__

#include <cmath>

namespace BigInt
{
	// Base 2^32 (Uint32)
	class Unsigned
	{
		unsigned num_digitos; // Contador de dígitos
		vector<unsigned>;
	};
};

#endif
